# Dependencies for the RunPod GPU pod only.
# The pod runs: moshi.server (PersonaPlex WS) + presonaplex.py serve-http (HTTP bridge)

fastapi>=0.115.0
uvicorn>=0.30.0
websockets>=12.0
httpx>=0.27.0
pydantic>=2.7.0
python-dotenv>=1.0.0
runpod>=1.7.0
opuslib>=3.0.1
av>=16.0.0
